/**
 * Default (production) configuration. `angular.json` swaps this file for
 * `environment.development.ts` during a development build.
 */
export const environment = {
  apiBaseUrl: 'http://localhost:3000/api',
};
